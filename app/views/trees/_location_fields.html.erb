
  <p>
    <%= location_fields.label :nearest_address %>
    
    <%= location_fields.text_field :nearest_address %>
    <% if @tree.new_record? -%>
      <span class="note">A postal address in the UK (without the postcode). Computer will use this to put it roughly on a map</span>
    <% end -%>
    </p>

  

  <p>
    <%= location_fields.label :postcode %>
    <%= location_fields.text_field :postcode %>
  </p>

    <p>
    <%= location_fields.label :how_to_find %>
    <%= location_fields.text_field :how_to_find %>
    <span class="note">Notes for a human on the ground starting from the nearest address.</span> 
  </p>

  <div id="coordinates">
    <h3>Coordinates</h3>     
  
  <p><%= location_fields.label :latitude %>  <%= location_fields.text_field :lat %>
  </p>
  <p>
    <%= location_fields.label :longditude %>  <%= location_fields.text_field :lng %>
  </p>
  <% if @tree.new_record?%><p class="note">Leave these blank and they will be looked up from the address & postcode. </p><% end -%>
	<p class="note">
      At present if you want to change the location of the tree you need to find out the latitude/Longditude yourself.
      There's a tool here: <%= link_to nil, "http://www.gorissen.info/Pierre/maps/googleMapLocation.php"%>
    </p>
  
</div>

   



